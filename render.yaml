# Render Blueprint – deploy Smart Code Reviewer as a free Web Service
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: smart-code-reviewer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn smart_code_reviewer.app:app --host 0.0.0.0 --port $PORT
