# Fly.io – deploy with: fly launch (first time) then fly deploy
# Docs: https://fly.io/docs/languages-and-frameworks/dockerfile/

app = "smart-code-reviewer"

[build]

[env]
  PORT = "8000"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
